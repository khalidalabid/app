apply plugin: 'com.android.application'

android {
    signingConfigs {
        config {
            keyAlias 'Imakkan'
            keyPassword 'hardrock'
            storeFile file('C:/Users/khaid/AndroidStudioProjects/app/ImakkanKey')
            storePassword 'hardrock'
        }
    }
    compileSdkVersion 23
    buildToolsVersion "23.0.1"
    defaultConfig {
        applicationId "com.imakkan.app"
        minSdkVersion 15
        targetSdkVersion 23
        versionCode 1
        versionName "1.0"
        signingConfig signingConfigs.config
    }
    buildTypes {
        release {
            minifyEnabled true
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    productFlavors {
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.support:appcompat-v7:23.0.1'
}
